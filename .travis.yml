language: node_js
node_js:
  - "10.9"
dist: trusty
sudo: false

cache:
  directories:
    - node_modules

addons:
  chrome: stable

script:
  - npm run build --prod
  
deploy:
  provider: firebase
    token:
      secure: $FIREBASE_TOKEN